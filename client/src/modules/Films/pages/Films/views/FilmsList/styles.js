const styles = theme => ({
    progress: {
        margin: theme.spacing.unit * 2,
    },
    container: {
    },
    list: {
        width: '100%'
    },
    image: {
        width: 400,
        height: 'auto'
    }
});

export default styles;