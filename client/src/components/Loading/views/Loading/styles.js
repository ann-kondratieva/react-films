const styles = theme => ({
    progress: {
        margin: theme.spacing.unit * 2,
    },
    container: {
        paddingTop: 150,
        paddingLeft: 300,
        paddingRight: 300
    }
});

export default styles;